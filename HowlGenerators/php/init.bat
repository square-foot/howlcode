@ECHO OFF
@REM This batch file is invoked just before generation. The full -dirbuild folder is passed to this batch file
@REM Use this to do some initialization of the backend server, if so required
@REM In case of some backends; e.g. PHP and Pocketbase, there is nothing to be done with this batch file
